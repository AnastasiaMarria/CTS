package cts.test.design.patterns.structurals.composite;

public interface InterfaceNPC {
    public void move(int distance); //method to move the NPC by a certain distance
    public void shoot(int power);  //method to make the NPC shoot by a specified power
}
